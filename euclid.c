#include<stdio.h>
#include<math.h>

float v1[20], v2[20];
int i, j;

float distancia(float v1[] float v2[] int n){
    float soma=0;
    for(i=0; i<n; i++){
      soma += (v1[i]-v2[i])*(v1[i]-v2[i]);
    }
    return sqrt(soma);
}

void calcula_produto(float M[20][20]
                     float x[20]
                     float vetR[20]
                     int n)

int main(){
  int n;
  float n2, n3, A[20][20], x[20], b[20];



  return 0;
}
