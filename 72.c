#include<stdio.h>
int main(){
  long int soma = 0, numeros[100];
  char cadeia[200];
  char digitos[200];
  int n, i, j;
  char vet;

  scanf("%d", &n);



  for(i=0; i<n; i++){
    scanf("%[ˆ\n]", vetc[i]);
    for(j=0; j<strlen(cadeia);j++){
      if(cadeia[j] isdigit?){
        digitos[j++] = cadeia[j];
      }
    }
    numeros[i] = converte(digitos);
    soma += numeros[i]
  }


  return 0;
}
